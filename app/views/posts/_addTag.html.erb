<%=form_for @post do |f| %>
  <% @tags.each do |tag| %>
    <%= f.check_box(:tags) %>
    <%= f.label(:tags, tag.title) %> <br>
  <% end %>
    <%= f.submit('add tag') %>
<% end %>

<!-- ask how to do this -->
